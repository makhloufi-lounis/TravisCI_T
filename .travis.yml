language: python
python:
  - "3.4"
#install:
  #- pip install -r requirements.txt
# command to run tests
script:
  - python3 test_fruits.py
 
#safelist
branches:
  only:
  - master
  - /[upt][a-q][t]\b//
  
#blocklist
branches:
  except:
  - opt
 
 #comments
 
